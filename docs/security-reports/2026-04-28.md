# Security Review — 2026-04-28

## Scope

Comprehensive daily security audit of the WineBox project (FastAPI + MongoDB wine cellar application) covering:

1. Dependency vulnerability check
2. Hardcoded secrets scan
3. Authentication & authorization audit
4. NoSQL injection & input validation audit
5. Rate limiting & DoS prevention
6. Session & token security
7. Security headers & CORS
8. Data exposure review
9. Git history review
10. Infrastructure & configuration
11. Third-party integration security

---

## Findings

### CRITICAL

None.

### WARNING

None.

### INFO (Best practice recommendations)

**INFO-1: E2E test secret key hardcoded in tasks.py** *(carried from 2026-04-26)*

- **File:** `tasks.py` (lines 282, 1965)
- **Values:** `"e2e-test-secret-key-not-for-production-use-1234567890"` and `"oat-e2e-test-secret-key-1234567890"`
- **Detail:** Clearly labelled test-only values used for isolated E2E and OAT test environments. Not used in production. Low risk.
- **Recommendation:** Consider loading from an environment variable for consistency with project conventions.

**INFO-2: Admin panel user-modification endpoints lack explicit rate limits**

- **File:** `winebox/admin/routers/admin.py` (lines 234, 243, 253, 262, 272, 282)
- **Detail:** Endpoints for activate/deactivate/make-admin/remove-admin/verify/delete user have no explicit rate limits. These are protected by `RequireAdmin` dependency and IP restriction at the nginx layer, so the blast radius is small.
- **Recommendation:** Add `@limiter.limit("10/minute;50/hour")` to user-modification admin endpoints for defence-in-depth.

**INFO-3: Import batch processing endpoints lack explicit rate limits**

- **File:** `winebox/routers/import_router.py` (lines 263, 318, 360, 409, 450, 627)
- **Detail:** Append-rows, set-mapping, process, process-stream, and remap endpoints have no explicit rate limits. They are protected by ownership checks (`_get_user_batch()`) and the upload endpoint is rate-limited (10/min, 30/hr), so abuse requires authentication.
- **Recommendation:** Add `@limiter.limit("30/minute;200/hour")` to batch processing endpoints for defence-in-depth.

---

### Previous INFO items resolved since 2026-04-26

| # | Issue | Status |
|---|-------|--------|
| INFO-1 | SECURITY.md mentions nonces for inline scripts | **FIXED** — CSP description now accurately says `script-src 'self'` |
| INFO-2 | Nginx does not set security headers for static file locations | **FIXED** — `X-Content-Type-Options` and `X-Frame-Options` added to all static locations |
| INFO-3 | Password strength validation is length-only | **FIXED** — Password strength feedback added (commits a8665af, bba5c02) |
| INFO-4 | Shell command injection risk in invoke tasks | **FIXED** — `shlex.quote()` applied consistently (commit a3b6806) |

---

### CLEAN (Passed review)

#### 1. Dependency Vulnerability Check

All key dependencies are at patched versions:

| Package | Locked Version | Status |
|---------|---------------|--------|
| fastapi | 0.128.1 | Current, no known CVEs |
| uvicorn | 0.40.0 | Current, no known CVEs |
| pydantic | 2.12.5 | Current (CVE-2026-25580 affects pydantic-ai, not pydantic core) |
| pymongo | 4.16.0 | Current, no known CVEs |
| pillow | 12.2.0 | Patched for CVE-2026-25990 and CVE-2026-40192 |
| python-multipart | 0.0.26 | Patched for CVE-2026-40347 |
| jinja2 | 3.1.6 | Current, no known CVEs in jinja2 itself |
| bcrypt | 5.0.0 | Current, no known CVEs |
| pyjwt | 2.12.1 | Patched for CVE-2026-32597 (crit header bypass) |
| cryptography | 46.0.7 | Patched for CVE-2026-39892, CVE-2026-34073, and CVE-2026-26007 |
| anthropic | 0.96.0 | Current (CVE-2026-34450/34452 affect memory tool, not used by WineBox) |
| slowapi | 0.1.9 | Current, no known CVEs |
| openpyxl | 3.1.5 | Current, no known CVEs |
| posthog | 7.13.0 | Current, no known CVEs |
| cairosvg | 2.9.0 | Patched for CVE-2026-31899 (recursive use DoS) |
| aioboto3 | 15.5.0 | Current, no known CVEs |
| httpx | 0.28.1 | Current, no known CVEs |
| fastapi-users | 15.0.4 | Current, no known CVEs |
| pydantic-settings | 2.12.0 | Current, no known CVEs |

No dependencies are yanked or more than 2 major versions behind.

#### 2. Hardcoded Secrets Scan

- No API keys, connection strings, passwords, private keys, or JWTs found in source code.
- `config/secrets.env.example` contains only placeholder values.
- `.gitignore` properly covers: `.env`, `secrets.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`.
- `git ls-files` confirms no sensitive files tracked (only `config/secrets.env.example`).
- Test fixtures use obvious dummy values (`sk-ant-api-key`, `phc_test_api_key`, `AKIAIOSFODNN7EXAMPLE`).
- GitHub Actions workflows use `${{ secrets.NAME }}` pattern throughout.

#### 3. Authentication & Authorization Audit

- **All 60+ endpoints** handling user data require `RequireAuth` or `RequireAdmin`.
- **All database queries** for user-owned data filter by `owner_id` or `cellar_id`.
- Public endpoints (`/api/reference/*`, `/api/xwines/*`, `/health`, `/api/config/analytics`) serve only static reference data — no user information exposed.
- Admin endpoints use `RequireAdmin` (not just `RequireAuth`).
- Admin panel login explicitly checks `is_superuser` before granting access.
- Admin self-protection: prevents deactivating self, removing own admin, deleting own account.
- Password changes invalidate all existing tokens via dual mechanism:
  - Per-token blacklist (JTI-based)
  - Bulk `tokens_invalidated_after` cutoff on user document
- Password reset also triggers full token invalidation.
- User registration validates email format (Pydantic `EmailStr`) and password length (8+ chars).
- Password strength feedback displayed to users in UI.
- Constant-time password comparison via Argon2 `verify()` with dummy hash for non-existent users.
- Account lockout after 5 failed attempts (15-minute window).

#### 4. NoSQL Injection & Query Safety

- All regex searches use `re.escape()` before compilation.
- No unsafe MongoDB operators (`$where`, `$expr`, `$accumulator`) found.
- All `ObjectId` parsing wrapped in `try/except` with proper 400/404 responses.
- All API inputs validated via Pydantic models — no raw `dict` or untyped JSON bodies.
- No raw pymongo collection access bypassing model safety.
- Search query length capped at 200 characters.
- `$in` queries bounded by `MAX_USER_RESULTSET` or fixed limits.

#### 5. Input Validation & Injection Prevention

- File uploads validated: magic byte detection, extension whitelist, size limits (configurable, import capped at 25 MB).
- Path traversal prevented in image serving (`_safe_resolve()` rejects `..` components).
- Generated filenames use UUIDs, not user input.
- Jinja2 templates use `autoescape=True` — no `|safe` or `{% autoescape false %}` usage.
- Static site export uses custom `escapeHtml()` function for all user-generated content.
- No `eval()`, `exec()`, `__import__()`, or `compile()` in application code.
- All paginated endpoints enforce maximum limits via `Query(ge=1, le=MAX)`.

#### 6. Rate Limiting & DoS Prevention

- **Global:** 60 requests/minute per IP.
- **Login:** 30/minute, 200/hour + account lockout after 5 failed attempts (15-minute window).
- **Registration:** 10/minute, 50/hour.
- **Password reset/change:** 5/minute, 20/hour.
- **Email verification:** 5/minute request, 10/minute verify.
- **Search:** 120/minute, 2000/hour with 200-char query limit.
- **Export:** 10/minute, 30/hour.
- **Import upload:** 10/minute, 30/hour.
- **Scan/enrichment:** 20/minute, 100-200/hour.
- **Admin panel login:** 5/minute.
- **Admin info/users/stats:** 30/minute.
- All paginated queries bounded by `MAX_USER_RESULTSET`.

#### 7. Session & Token Security

- JWT tokens use HS256 with minimum 32-character secret key (enforced at startup).
- Tokens include `jti` (UUID), `iat`, `exp`, and `sub` claims.
- Access token lifetime: 120 minutes.
- Dual-layer revocation: per-token blacklist + bulk user invalidation cutoff.
- Revoked token cleanup via background task (hourly).
- Purpose-specific secret derivation (SHA256) for reset/verification tokens prevents type confusion.
- No token values logged anywhere in the codebase.
- HSTS enabled in production: `max-age=31536000; includeSubDomains; preload`.

#### 8. Security Headers

- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `X-XSS-Protection: 1; mode=block`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `X-Permitted-Cross-Domain-Policies: none`
- `Permissions-Policy` restricting camera, geolocation, microphone, etc.
- `Content-Security-Policy`: `script-src 'self'` (no unsafe-inline for scripts), PostHog domains whitelisted.
- CORS: empty origin list by default (same-origin only), explicit whitelist when configured.
- Nginx: `server_tokens off`, security headers on static file locations.

#### 9. Data Exposure

- `UserResponse` model excludes `hashed_password` and internal fields.
- Error messages are generic ("Incorrect email or password") — no user enumeration.
- API docs (`/docs`, `/redoc`, `/openapi.json`) disabled in production.
- No stack traces, database names, or file paths in error responses.
- PostHog public API key properly separated from server-side key.
- Price tracker PII gated by ownership checks.

#### 10. Git History Review

- Last 30 commits: admin panel wiring, dark mode fix, security review adoption, version bumps. No security-concerning changes.
- No secret files accidentally committed (`git log --diff-filter=A` clean).
- Security-related changes since last review limited to admin panel deployment (`deploy/`, `winebox/admin/`) — properly uses `RequireAdmin` and IP restriction.
- `config/secrets.env.example` is the only secrets-adjacent file in git (contains only placeholders).

#### 11. Infrastructure & Configuration

- Production safety checks at startup: weak secret key blocked, localhost MongoDB warned, HTTPS enforcement checked, debug mode rejected.
- Database safety check prevents dev environments from connecting to production MongoDB via FQDN verification.
- Nginx: `server_tokens off`, TLS 1.2/1.3 only, strong ciphers, session tickets disabled, HSTS 2-year max-age.
- Admin panel IP-restricted at nginx layer with allowlist from `deploy/winebox-admin.toml`.
- Admin panel runs on separate port (8001) behind dedicated upstream.
- All Anthropic API calls have explicit timeouts (60-120 seconds).
- All external API calls have timeout configuration.

#### 12. Third-Party Integration Security

- Anthropic API key loaded from environment/secrets, not hardcoded.
- PostHog API key properly separated (public key in config, no server-side secrets in client code).
- No webhook endpoints requiring signature validation.
- AWS SES credentials loaded from secrets, not hardcoded.

---

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| WARNING | 0 |
| INFO | 3 |
| CLEAN | 12 categories |

**Date of review:** 2026-04-28
**Reviewer:** Automated security audit (Claude Code)
**Codebase version:** 0.7.79 (commit a3b6806)

### Comparison with previous review (2026-04-26)

- Previous review: 0 CRITICAL, 0 WARNING, 5 INFO
- This review: 0 CRITICAL, 0 WARNING, 3 INFO
- **4 of 5 previous INFO items resolved** (SECURITY.md nonces, nginx static headers, password strength feedback, shlex.quote)
- 1 INFO item carried forward (E2E test secret key)
- 2 new INFO items identified (admin rate limits, import batch rate limits)

### Top 3 Priorities

1. Add explicit rate limits to admin user-modification endpoints (INFO-2) — defence-in-depth.
2. Add explicit rate limits to import batch processing endpoints (INFO-3) — defence-in-depth.
3. Load E2E test secret keys from environment variables (INFO-1) — consistency.

**Overall assessment: Clean bill of health.** The WineBox codebase demonstrates strong security practices across authentication, authorization, input validation, dependency management, and infrastructure configuration. No critical or warning-level issues found. Four of five previous recommendations have been addressed since the last review.
